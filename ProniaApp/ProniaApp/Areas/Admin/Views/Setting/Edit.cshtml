@model Setting
@{
    ViewData["Title"] = "Edit";
}
@section Css{
    <link rel="stylesheet" href="~/admin/assets/css/index.css">
}
<section class="pcoded-main-container">
    <div class="pcoded-content">
        <div class="row">
            <div class="col-12">
                <span class="text-danger">@ViewBag.error</span>
                <form method="post" enctype="multipart/form-data" asp-action="Edit">
                    @if (Model.Value.Contains(".png") || Model.Value.Contains(".jpg") || Model.Value.Contains(".jpeg"))
                    {
                        <div class="image"><img class="img-fluid" src="~/assets/images/logo/@Model.Value" /></div>
                        <div class="form-group">
                            <input class="form-control" asp-for="LogoPhoto">
                        </div>
                    }
                    else
                    {
                        <div class="form-group">
                            <input class="form-control" asp-for="@Model.Value">
                        </div>
                    }
                    <button type="submit" class="btn btn-success"><i class="feather mr-2 icon-check-circle"></i>Submit</button>
                    <a asp-action="Index" class="btn btn-danger"><i class="feather mr-2 icon-check-circle"></i>Cancel</a>
                </form>

            </div>
        </div>

    </div>
</section>



